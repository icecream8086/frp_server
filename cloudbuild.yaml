steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [
    'build',
    '-t', 'us-central1-docker.pkg.dev/${PROJECT_ID}/my-docker-repo/frps',
    '-f', 'dockerfiles/Dockerfile-for-frps',
    '.'
  ]
options:
  logging: CLOUD_LOGGING_ONLY
